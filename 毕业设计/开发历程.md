
tar -cJf 2023.tar.xz  2023/

scp -C -o Cipher=aes128-gcm@openssh.com \
    tencent01:/root/news_data/2023.tar.xz \
    debian:/root/news_data/

apt install -y xz-utils

1. 先把前后端打通，确定模板
2. 开发存储系统

使用apifox编写接口文档，后续方便ai生成后端和前端代码

改造rust后端模板，设计项目架构

同一个路径的不同方法接口需要放在一起文件，管理更清晰
普通请求使用Access Token Authorization: Bearer

Ctrl + Shift + P  >keyboard
cursor中无法绑定ctrl+. 
{
  "key": "alt+.",
  "command": "editor.action.quickFix",
  "when": "editorHasCodeActionsProvider && editorTextFocus && !editorReadonly"
}
ctrl m 0 j

确认后端框架是否正确

使用wsl：ubuntu，进入目标文件夹
sudo apt install tree
wsl -d Ubuntu --user root
passwd yourname

完善demo，跑通

debian服务器和云服务器安装数据库
安装podman
podman安装pg
安装redis

设置vmware网络模式从NAT切换为Bridged

无线局域网适配器 WLAN:

   连接特定的 DNS 后缀 . . . . . . . :
   本地链接 IPv6 地址. . . . . . . . : fe80::f724:b3e4:bd5f:f61%8
   IPv4 地址 . . . . . . . . . . . . : 192.168.0.100
   子网掩码  . . . . . . . . . . . . : 255.255.255.0
   默认网关. . . . . . . . . . . . . : 192.168.0.1

[Match]
Name=ens33

[Network]
Address=192.168.248.200/32
Gateway=192.168.0.1
DNS=8.8.8.8
DNS=8.8.4.4


apt install docker.io -y
开发环境中的pg一直连不上，换一个ubuntu试试
ubuntu可行
cat id_ed25519.pub | ssh root@192.168.0.200 "mkdir -p ~/.ssh && chmod 700 ~/.ssh && cat >> ~/.ssh/authorized_keys && chmod 600 ~/.ssh/authorized_keys"

配置redis开发环境

创建用户表
实现注册功能，邮箱验证码rand+Redis+lettre发送邮件，有效期，一次性，限流(1分钟一次，一小时最多5次)，雪花算法，密码加密-argon2 
实现登录功能，现实现密码登录，后续考虑验证码登录

完成基础用户功能

存储opensearch

使用docker搭建了opensearch

制定详细存储策略

使用pg存储元数据

选取向量模型

创建索引

插入数据

rust embed crate

embed fastapi

完成search
BM25 解析失败 — insert.py 中 id 和 doc_id 均以 str(doc_id) 写入，字段类型从 i32 改为 String，在 rrf_merge 中用 .parse::<i32>() 转换
num_candidates 未知字段 — 该参数是 Elasticsearch 8.x 专有，OpenSearch 不支持，已从 kNN 查询中移除
测试构造体字面量 — 同步将测试中的 id: 1 / doc_id: 2 改为 "1".to_string() / "2".to_string()

前端开发





```
路由守卫：
import { useAuthStore } from '@/stores/auth'

let initPromise: Promise<void> | null = null

async function ensureAuthInit() {
  const authStore = useAuthStore()

  if (!initPromise) {
    initPromise = authStore.initAuth()
  }

  return initPromise
}

router.beforeEach(async (to, from, next) => {
  const authStore = useAuthStore()

  // ✅ 确保初始化完成（无轮询）
  await ensureAuthInit()

  if (to.meta.requiresAuth && !authStore.isLoggedIn) {
    return next('/login')
  }

  next()
})

拦截器：
import { useAuthStore } from '@/stores/auth'

axios.interceptors.response.use(
  res => res,
  async err => {
    const authStore = useAuthStore()

    if (err.response?.status === 401) {
      const success = await authStore.safeRefresh()

      if (success) {
        return axios(err.config)
      } else {
        authStore.logout()
        window.location.href = '/login'
      }
    }

    return Promise.reject(err)
  }
)
```





# CSRF 防护（你用 cookie 必须做）

因为你把 refreshToken 放 cookie：

### ❗风险

```
CSRF 攻击（跨站请求伪造）
```

------

### ✅ 解决方案

#### 方案 1（推荐）

```
SameSite=Strict 或 Lax
```

#### 方案 2（更安全）

```
CSRF Token（双重校验）
```



权限系统（RBAC）

企业级基本都会有：

```
userInfo: {
  roles: ['admin'],
  permissions: ['user:create']
}
```

路由控制：

```
if (!hasPermission(to.meta.permission)) {
  return next('/403')
}
```

------

# 9️⃣ 请求层解耦（非常重要）

👉 不要在组件里直接写 axios

结构：

```
api/
  auth.ts
  user.ts
  product.ts
```

------

# 🔟 错误体系统一（可观测性）

统一处理：

```
switch (error.code) {
  case 'TOKEN_EXPIRED':
  case 'UNAUTHORIZED':
}
```

------

# 四、安全再强化（高级）

如果你做 SaaS 或金融系统：

------

## ✅ 设备绑定

```
refreshToken 绑定 deviceId
```

------

## ✅ IP / 地区检测

```
异常登录 → 强制重新登录
```

------

## ✅ 单点登录（SSO）

结合 OAuth 2.0 / OpenID Connect

------

# 五、最终“企业级架构图”

```
[Vue App]
   ↓
[Pinia 内存状态]
   ↓
[Axios 拦截器]
   ↓
401 → refresh 队列
   ↓
[refreshToken (cookie)]
   ↓
[后端认证系统]
```



window.location.href = "/login";需要修改为路由跳



使用nginx代理解决https和跨域问题



![image-20260331171307721](C:\MY\md\tutuLP.github.io\毕业设计\images\开发历程.assets\image-20260331171307721.png)

一周6000条，太慢了

换到windows上使用cuda试试







![image-20260401155407978](C:\MY\md\tutuLP.github.io\毕业设计\images\开发历程.assets\image-20260401155407978.png)

![image-20260401163652225](C:\MY\md\tutuLP.github.io\毕业设计\images\开发历程.assets\image-20260401163652225.png)

![image-20260407200834790](C:\MY\md\tutuLP.github.io\毕业设计\images\开发历程.assets\image-20260407200834790.png)

1:1换为加权3:7

![image-20260407201334915](C:\MY\md\tutuLP.github.io\毕业设计\images\开发历程.assets\image-20260407201334915.png)
